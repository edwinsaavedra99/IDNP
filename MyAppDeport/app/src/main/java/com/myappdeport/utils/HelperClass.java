package com.myappdeport.utils;

import android.util.Log;

import static com.myappdeport.utils.Constants.*;

public class HelperClass {
    public static void logErrorMessage(String errorMessage) {
        Log.d(TAG, errorMessage);
    }
}